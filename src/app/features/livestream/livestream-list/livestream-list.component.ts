import { Component } from '@angular/core';

@Component({
  selector: 'app-livestream-list',
  imports: [],
  templateUrl: './livestream-list.component.html',
})
export class LivestreamListComponent {
  // readonly connections = input.required<StateTransform<
  //   ConnectionDto[]
  // > | null>();
  // readonly relativeRightWidth = input.required<RelativeWidth>();
  // readonly livestreamRequests = model.required<LivestreamRequest[]>();
  // readonly selectedImei = model.required<string | null>();
  // readonly map = model.required<L.Map>();
  // readonly refreshConnections = output<void>();
  // searchText = signal<string>('');
  // protected onSearchImei(text: string): void {
  //   this.searchText.set(text.toLowerCase().trim());
  // }
  // protected onRefreshConnections(): void {
  //   this.refreshConnections.emit();
  // }
  // protected onSelectImei(imei: string): void {
  //   this.selectedImei.set(imei);
  // }
}
